// Simulator is a command line tool to simulate the behavior of a
// set of battery sensors. It generates configurable patterns battery
// state events and sends them to the BMS server.

package main

import (
	"flag"
)

